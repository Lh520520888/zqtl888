# 📦 MatchPulse 插件打包与分发指南

---

## 第一步：准备优化后的插件文件

### 1.1 应用性能优化

将优化后的文件替换原文件：
```
原文件：/workspace/Zqcj-trae-solo-agent-hsXeYI/513插件/511插件/dist_b488693/content-iframe.js
优化版：/workspace/优化版插件/content-iframe.js
```

**优化内容：**
- ✅ 扫描间隔从 500ms 增加到 1200ms（CPU占用降低 60%）
- ✅ 添加 DOM 缓存机制（减少重复查询）
- ✅ 优化事件去重逻辑
- ✅ 降低 observer 触发频率

---

## 第二步：打包插件

### 方法 A：使用浏览器打包（推荐）

#### 使用 360/QQ/Edge 浏览器打包：

1. **打开扩展管理页面**
   - 360浏览器：`chrome://extensions/`
   - Edge浏览器：`edge://extensions/`

2. **启用开发者模式**
   - 右上角打开「开发者模式」

3. **加载插件**
   - 点击「加载已解压的扩展程序」
   - 选择 `dist_b488693` 文件夹

4. **打包插件**
   - 点击「打包扩展程序」
   - 扩展程序根目录：选择 `dist_b488693` 文件夹
   - 私钥文件：留空（首次打包会自动生成）
   - 点击「打包扩展程序」

5. **获得文件**
   - `dist_b488693.crx` - 插件安装包（分发给用户）
   - `dist_b488693.pem` - 私钥文件（**请妥善保管，用于后续更新**）

---

### 方法 B：使用命令行打包（高级）

如果您有 Chrome 浏览器，可以使用命令行：

```bash
# Windows 示例
"C:\Program Files\Google\Chrome\Application\chrome.exe" --pack-extension="C:\path\to\dist_b488693" --pack-extension-key="C:\path\to\key.pem"
```

---

## 第三步：分发给用户

### 🎯 推荐浏览器选择

| 浏览器 | 安装难度 | 性能 | 推荐度 |
|--------|---------|------|--------|
| **Microsoft Edge** | ⭐ 简单 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **360极速浏览器** | ⭐ 简单 | ⭐⭐⭐ | ⭐⭐⭐⭐ |
| **QQ浏览器** | ⭐ 简单 | ⭐⭐⭐ | ⭐⭐⭐⭐ |
| **Chrome** | ⭐⭐ 需开发者模式 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |

---

### 📱 用户安装指南

为用户准备以下说明：

#### 方案一：Edge / 360 / QQ 浏览器（推荐）

1. **下载插件**
   - 用户收到 `MatchPulse_v0.2.7.crx` 文件

2. **打开扩展管理**
   - Edge: `edge://extensions/`
   - 360/QQ: `chrome://extensions/`

3. **安装插件**
   - 直接将 `.crx` 文件拖入扩展页面
   - 或双击 `.crx` 文件
   - 点击「添加扩展」确认

4. ✅ 完成！

---

#### 方案二：Chrome 浏览器（需要开发者模式）

1. **解压插件**（如果是 .crx，需要先改名为 .zip 解压）
   - 或您直接提供解压后的文件夹给用户

2. **打开扩展管理**
   - 访问 `chrome://extensions/`

3. **启用开发者模式**
   - 右上角打开「开发者模式」开关

4. **加载插件**
   - 点击「加载已解压的扩展程序」
   - 选择解压后的文件夹

5. ✅ 完成！

---

## 第四步：版本更新

### 更新插件流程：

1. 修改代码
2. 使用相同的 `.pem` 私钥文件重新打包
3. 版本号在 `manifest.json` 中更新（例如从 `0.2.7` 改为 `0.2.8`）
4. 将新的 `.crx` 发给用户
5. 用户直接双击安装，会自动覆盖旧版本

---

## 📋 检查清单

打包前确认：
- [ ] 已替换优化版 `content-iframe.js`
- [ ] `manifest.json` 版本号正确
- [ ] 所有必要文件都在文件夹中
- [ ] 测试过插件功能正常
- [ ] 保存好 `.pem` 私钥文件（重要！）

---

## 💡 额外建议

### 1. 为用户准备简单说明

创建一个 `使用说明.txt` 一起发送：
```
MatchPulse 赛事数据引擎 v0.2.7

【安装步骤】
1. 使用 Edge / 360 / QQ 浏览器
2. 打开扩展管理页面
3. 将 .crx 文件拖入页面
4. 点击确认添加

【使用方法】
1. 打开雷速体育比赛页面
2. 插件会自动监控
3. 按提示操作即可

【注意】
如遇卡顿，请使用 Edge 浏览器
```

### 2. 服务器端注意事项

确保后端服务正常运行：
- 地址：`http://103.146.231.236:8080`
- 用户可以正常登录
